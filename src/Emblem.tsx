// The emblem generator is generic (colors + geometric pattern), so it's
// reused here under a neutral name for the Politician & Nation test.
export { default } from './PhilosorterEmblem'
export type { Pattern } from './philosorterPhilosophies'
